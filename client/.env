cd ~/workspace

cat > client/.env <<'EOF'
# For Replit (frontend+backend served together), leave blank to use same-origin.
# For Railway separated frontend, set it to: https://<your-backend-domain>
VITE_API_BASE_URL=
EOF
