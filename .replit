modules = ["nodejs-22", "web"]

run = "bash start-replit.sh"

hidden = [
  ".config",
  ".git",
  "generated-icon.png",
  "node_modules",
  "client/node_modules",
  "dist",
  "client/dist",
]

[nix]
channel = "stable-24_05"

[env]
HOST = "0.0.0.0"

[deployment]
deploymentTarget = "run"

[[ports]]
localPort = 8080
externalPort = 80
