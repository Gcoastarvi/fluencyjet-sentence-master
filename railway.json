{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "ignoreDockerfile": true
  },
  "nixpacks": {
    "phases": {
      "setup": {
        "nixPkgs": ["nodejs_22_12", "npm_9_x", "openssl"]
      },
      "install": {
        "cmds": [
          "echo '✅ Custom install phase running...'",
          "npm install --no-audit --no-fund"
        ]
      },
      "build": {
        "cmds": [
          "echo '✅ Custom build phase running...'",
          "cd client && npm install --no-audit --no-fund && npm run build && cd .. && npx esbuild server/index.ts --platform=node --bundle --format=esm --outfile=dist/index.js"
        ]
      }
    }
  },
  "deploy": {
    "startCommand": "node dist/index.js"
  }
}
